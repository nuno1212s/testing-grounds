# Empty stub. The shared Makefile requires each project's bench dir to exist; this
# project has no remote/Docker deployment target (in-process harness). Left empty
# deliberately — do not add hosts here.
